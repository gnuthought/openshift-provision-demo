---
demo_default_cluster_resources:
- ClusterRole/kube-cluster-autoscaler.yml

demo_default_cluster_role_bindings:
  cluster-admin:
    users:
    - system:admin
    - system:serviceaccount:openshift-provision:manager
    groups:
    - system:cluster-admins
    - system:masters
    remove_unlisted: true
  kube-cluster-autoscaler:
    users:
    - system:serviceaccount:kube-cluster-autoscaler:default
    remove_unlisted: true
  self-provisioner:
    users:
    - system:serviceaccount:management-infra:management-admin
    groups:
    - system:serviceaccounts:openshift-provision
    remove_unlisted: false

demo_default_process_templates:
- file: https://raw.githubusercontent.com/gnuthought/openshift-provision-manager/master/install-template-admin.yaml
- file: https://raw.githubusercontent.com/gnuthought/openshift-provision-example-0/master/install-template-admin.yaml
- file: https://raw.githubusercontent.com/gnuthought/openshift-provision-example-1/master/install-template-admin.yaml
- file: https://raw.githubusercontent.com/gnuthought/openshift-provision-example-2/master/install-template-admin.yaml

demo_default_projects:
  default: "{{ demo_project_default }}"
  kube-cluster-autoscaler: "{{ demo_project_kube_cluster_autoscaler }}"
  kube-public: "{{ demo_project_kube_public }}"
  openshift: "{{ demo_project_openshift }}"
  openshift-provision: "{{ demo_project_openshift_provision }}"

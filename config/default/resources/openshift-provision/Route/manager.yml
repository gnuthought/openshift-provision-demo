---
apiVersion: v1
kind: Route
metadata:
  name: manager
spec:
  port:
    targetPort: api
  tls:
    termination: edge
  to:
    kind: Service
    name: manager
    weight: 100
  wildcardPolicy: None

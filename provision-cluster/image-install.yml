---
# FIXME- Review this
- name: Load cluster config
  hosts: OSEv3
  vars:
    terraform_dir: "{{ playbook_dir }}/terraform"
  tasks:
  - import_tasks: ../load-config.yml

- import_playbook: "{{ openshift_ansible_path }}/playbooks/openshift-etcd/config.yml"
#- import_playbook: "{{ openshift_ansible_path }}/playbooks/openshift-nfs/config.yml"
#- import_playbook: "{{ openshift_ansible_path }}/playbooks/openshift-loadbalancer/config.yml"
- import_playbook: "{{ openshift_ansible_path }}/playbooks/openshift-master/config.yml"
- import_playbook: "{{ openshift_ansible_path }}/playbooks/openshift-master/additional_config.yml"
- import_playbook: "{{ openshift_ansible_path }}/playbooks/openshift-node/config.yml"

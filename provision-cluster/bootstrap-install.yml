---
- name: Prepare OSEv3 Hosts
  hosts: OSEv3
  pre_tasks:
  - name: Load dynamic config
    import_tasks: ../load-dynamic-config.yml
  roles:
  - role: node-prep

- import_playbook: bootstrap-install-v{{ openshift_release }}.yml
